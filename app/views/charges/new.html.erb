 <%= form_tag charges_path do %>
  <h4>Upgrade my subscription</h4>
  <p>$9.99 per month, cancel at any time.</p>

    <%= form_tag charges_path, method: :create do %>

    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button" data-key="<%= Rails.configuration.stripe[:publishable_key] %>" data-description="Monthly" data-email="<%= current_user.email %>" data-name="Wikibed Premium"  data-panel-label="Subscribe" data-amount="999" data-locale="auto" data-allow-remember-me=false></script>
    <% end %>
 <% end %>